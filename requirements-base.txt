# Everything except seocho. The seocho pin lives in requirements.txt (the tag the published
# results were produced against) and requirements-upstream.txt (upstream main, exercised by CI
# so a seocho change that breaks this experiment surfaces within a day, not at the next rerun).
duckdb>=0.10          # deterministic graph generation
pyarrow>=14           # Parquet snapshots the bulk loader reads
neo4j>=5.20           # bolt driver
openai-agents>=0.10   # the agent loop; also pulls openai
openai>=1.40
matplotlib>=3.8       # charts
pyyaml>=6
python-dotenv>=1.0
