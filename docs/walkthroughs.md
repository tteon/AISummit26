# Four real episodes, end to end

Generated by `scripts/dump_walkthroughs.py` from `results/agent_interaction.json`
— do not edit by hand. The charts say which design was right and which was slow;
these are the runs underneath. Every Cypher block is what the model actually
emitted; every db-hit count is measured.

The harness owns three parameters the model never writes: `$workspace_id` (tenant
scope), `$limit` (row cap) and `$a` (the account the question names). An anchor a
model inlines as a literal is the defect that turned one question into 38 million
db hits, so the harness supplies it.

---

## The prompt, per design

Condition 1 (`labels`) carries a 1,846-character prompt; condition 2 (`ontology`) carries 3,411. **The rules text is
identical** — the whole difference is the schema block. Conditions 3 and 4 add no
prompt text at all beyond one rule line for the plan gate: they add enforcement
*outside* the prompt. `docs/conditions.md` carries the full diff.

<details><summary>Condition 1 — the complete prompt</summary>

```
You are a financial-crime analyst answering questions about a financial graph by writing Cypher and running it with the run_cypher tool.

Schema:
{
  "nodes": {
    "Account": [
      "acct_no",
      "acct_type",
      "flagged",
      "iban",
      "id",
      "risk_tier"
    ],
    "Person": [
      "country",
      "id",
      "name"
    ],
    "Company": [
      "id",
      "name",
      "sector"
    ],
    "Loan": [
      "id",
      "principal"
    ],
    "Channel": [
      "code",
      "label",
      "risk_weight"
    ],
    "Medium": [
      "id",
      "risk_level",
      "type"
    ]
  },
  "relationships": {
    "TRANSFER": "(Account)-[:TRANSFER]->(Account)",
    "USES_CHANNEL": "(Account)-[:USES_CHANNEL]->(Channel)",
    "OWN": "(Person|Company)-[:OWN]->(Account)",
    "DEPOSIT": "(Loan)-[:DEPOSIT]->(Account)",
    "REPAY": "(Account)-[:REPAY]->(Loan)",
    "APPLY": "(Person|Company)-[:APPLY]->(Loan)",
    "INVEST": "(Person|Company)-[:INVEST]->(Company)",
    "GUARANTEE": "(Person|Company)-[:GUARANTEE]->(Person|Company)",
    "WITHDRAW": "(Account)-[:WITHDRAW]->(Account)",
    "SIGN_IN": "(Medium)-[:SIGN_IN]->(Account)"
  }
}

Rules:
- Every node pattern must carry the workspace scope, written exactly as {_workspace_id: $workspace_id}. The harness supplies $workspace_id, $limit and (where the question names an account) $a; you do not need to define them, and you must not inline their values.
- Refer to an account the question names by binding acct_no to the $a parameter, not by inlining the number.
- End every query with LIMIT $limit.
- Call the tool as many times as you need, then answer.

Finish your reply with a single line of the form:
ANSWER: <json>
where <json> is a JSON object for a question asking for named values, or a JSON array for a question asking for a list. Put nothing after that line.
```

</details>

<details><summary>Condition 2 — the complete prompt (same rules, richer schema)</summary>

```
You are a financial-crime analyst answering questions about a financial graph by writing Cypher and running it with the run_cypher tool.

Schema:
{
  "__tenant_scope__": [
    "every matched node must carry {_workspace_id: $workspace_id} inline in its pattern; do not introduce a workspace node or relationship"
  ],
  "Account": [
    "acct_no",
    "acct_type",
    "flagged",
    "iban",
    "id",
    "risk_tier"
  ],
  "Person": [
    "country",
    "id",
    "name"
  ],
  "Company": [
    "id",
    "name",
    "sector"
  ],
  "Loan": [
    "id",
    "principal"
  ],
  "Channel": [
    "code",
    "label",
    "risk_weight"
  ],
  "Medium": [
    "id",
    "risk_level",
    "type"
  ],
  "(Account)-[:TRANSFER]->(Account)": [
    "amount",
    "channel",
    "channel_risk",
    "cross_border",
    "ts"
  ],
  "__direction__(TRANSFER)": [
    "the arrow points from sender to beneficiary; follow it in the declared direction and do not use an undirected pattern unless the question is symmetric"
  ],
  "__cardinality__(TRANSFER)": [
    "heavy-tailed: median out-degree 3, p99 41, maximum 77,036. Multi-hop expansion from a high-degree node is unbounded work, so prefer a shape that can stop early (DISTINCT ... LIMIT) and avoid an unbounded aggregate or ORDER BY over the whole neighbourhood"
  ],
  "(Account)-[:USES_CHANNEL]->(Channel)": [
    "tx_count"
  ],
  "(Person|Company)-[:OWN]->(Account)": [],
  "(Loan)-[:DEPOSIT]->(Account)": [
    "amount"
  ],
  "(Account)-[:REPAY]->(Loan)": [
    "amount"
  ],
  "(Person|Company)-[:APPLY]->(Loan)": [],
  "__direction__(APPLY)": [
    "the arrow points from applicant to facility; follow it in the declared direction and do not use an undirected pattern unless the question is symmetric"
  ],
  "(Person|Company)-[:INVEST]->(Company)": [],
  "__direction__(INVEST)": [
    "the arrow points from investor to investee; follow it in the declared direction and do not use an undirected pattern unless the question is symmetric"
  ],
  "(Person|Company)-[:GUARANTEE]->(Person|Company)": [],
  "__direction__(GUARANTEE)": [
    "the arrow points from guarantor to guaranteed; follow it in the declared direction and do not use an undirected pattern unless the question is symmetric"
  ],
  "(Account)-[:WITHDRAW]->(Account)": [],
  "__direction__(WITHDRAW)": [
    "the arrow points from withdrawer to destination; follow it in the declared direction and do not use an undirected pattern unless the question is symmetric"
  ],
  "(Medium)-[:SIGN_IN]->(Account)": [],
  "__direction__(SIGN_IN)": [
    "the arrow points from device to session_account; follow it in the declared direction and do not use an undirected pattern unless the question is symmetric"
  ]
}

Rules:
- Every node pattern must carry the workspace scope, written exactly as {_workspace_id: $workspace_id}. The harness supplies $workspace_id, $limit and (where the question names an account) $a; you do not need to define them, and you must not inline their values.
- Refer to an account the question names by binding acct_no to the $a parameter, not by inlining the number.
- End every query with LIMIT $limit.
- Call the tool as many times as you need, then answer.

Finish your reply with a single line of the form:
ANSWER: <json>
where <json> is a JSON object for a question asking for named values, or a JSON array for a question asking for a list. Put nothing after that line.
```

</details>

---

## 1. The same question, four designs — `ext_med_1` at SF10

*"Which accounts sent money to account 0 on a transfer whose own `channel_risk`
is 5 or more? Give the five lowest account numbers."* The word *own* is the whole
question: `channel_risk` is a property of the TRANSFER relationship, and there is
also a `Channel` node carrying `risk_weight`. Nothing in a list of label names
says which one the question means.

### Condition 1 — labels

**labels** · `ext_med_1` · SF10 · anchor 0 — WRONG (f1 0.8), 2 round trips, 4432 ms wall

Reads the risk off the **Channel node** instead of the transfer, so it answers a different question — and gets a plausible, wrong list (missing account 0, adding account 3). This is the failure the ontology arm removes, and it is invisible without the gold.

*call 1 → `ok`*, 50 rows, 124,425 db hits, 57 ms

```cypher
MATCH (src:Account {_workspace_id: $workspace_id})-[:TRANSFER]->(tgt:Account {_workspace_id: $workspace_id, acct_no: $a})
MATCH (src)-[:USES_CHANNEL]->(ch:Channel {_workspace_id: $workspace_id})
WHERE ch.risk_weight >= 5
RETURN src.acct_no AS acct_no
ORDER BY src.acct_no ASC
LIMIT $limit
```

*call 2 → `ok`*, 50 rows, 119,440 db hits, 68 ms

```cypher
MATCH (src:Account {_workspace_id: $workspace_id})-[:TRANSFER]->(tgt:Account {_workspace_id: $workspace_id, acct_no: $a})
MATCH (src)-[:USES_CHANNEL]->(ch:Channel {_workspace_id: $workspace_id})
WHERE ch.risk_weight >= 5 AND src.acct_no <> $a
RETURN DISTINCT src.acct_no AS acct_no
ORDER BY src.acct_no ASC
LIMIT $limit
```

*final reply* (tail):

```
ANSWER: [1,2,3,4,5]
```

*verdict*: `{"correct": false, "f1": 0.8, "recall": 0.8, "precision": 0.8, "gold": ["0", "1", "2", "4", "5"]}`

---

### Condition 2 — ontology

**ontology** · `ext_med_1` · SF10 · anchor 0 — correct (f1 1.0), 2 round trips, 5747 ms wall

The schema block names `channel_risk` as a property of TRANSFER, so the model binds the relationship. Its first attempt is bad Cypher (a variable used outside its scope); the database's own error is enough to fix it.

*call 1 → `db_error`*, 0 rows, 0 db hits, 4 ms

```cypher
MATCH (sender:Account {_workspace_id: $workspace_id})-[:TRANSFER]->(target:Account {_workspace_id: $workspace_id, acct_no: $a})
WHERE EXISTS((sender)-[r:TRANSFER]->(target)) AND r.channel_risk >= 5
RETURN DISTINCT sender.acct_no AS acct_no
ORDER BY acct_no ASC
LIMIT $limit
```

*call 2 → `ok`*, 50 rows, 11,405 db hits, 32 ms

```cypher
MATCH (sender:Account {_workspace_id: $workspace_id})-[r:TRANSFER]->(target:Account {_workspace_id: $workspace_id, acct_no: $a})
WHERE r.channel_risk >= 5
RETURN DISTINCT sender.acct_no AS acct_no
ORDER BY sender.acct_no ASC
LIMIT $limit
```

*final reply* (tail):

```
ANSWER: [0,1,2,4,5]
```

*verdict*: `{"correct": true, "f1": 1.0, "recall": 1.0, "precision": 1.0, "gold": ["0", "1", "2", "4", "5"]}`

---

### Condition 3 — guardrail

**guardrail** · `ext_med_1` · SF10 · anchor 0 — correct (f1 1.0), 2 round trips, 4284 ms wall

Same query as condition 2 — the guardrail had nothing to reject here. The rulebook costs nothing when the model is already inside the contract.

*call 1 → `db_error`*, 0 rows, 0 db hits, 9 ms

```cypher
MATCH (sender:Account {_workspace_id: $workspace_id})-[:TRANSFER]->(target:Account {_workspace_id: $workspace_id, acct_no: $a})
WHERE EXISTS((sender)-[r:TRANSFER]->(target)) AND r.channel_risk >= 5
RETURN DISTINCT sender.acct_no AS acct_no
ORDER BY acct_no ASC
LIMIT $limit
```

*call 2 → `ok`*, 50 rows, 11,405 db hits, 8 ms

```cypher
MATCH (sender:Account {_workspace_id: $workspace_id})-[r:TRANSFER]->(target:Account {_workspace_id: $workspace_id, acct_no: $a})
WHERE r.channel_risk >= 5
RETURN DISTINCT sender.acct_no AS acct_no
ORDER BY sender.acct_no ASC
LIMIT $limit
```

*final reply* (tail):

```
ANSWER: [0,1,2,4,5]
```

*verdict*: `{"correct": true, "f1": 1.0, "recall": 1.0, "precision": 1.0, "gold": ["0", "1", "2", "4", "5"]}`

---

### Condition 4 — plan

**plan** · `ext_med_1` · SF10 · anchor 0 — correct (f1 1.0), 2 round trips, 3856 ms wall

Here the guardrail earns its place: the model invented a relationship variable `cr` inside a pattern, which is not in the ontology. The violation goes back as text and the rewrite is correct.

*call 1 → `guardrail_rejected`*, 0 rows, 0 db hits, 0 ms · violations: `unknown_relationships:cr`

```cypher
MATCH (target:Account {_workspace_id: $workspace_id, acct_no: 0})<-[:TRANSFER {channel_risk: cr}]-(sender:Account {_workspace_id: $workspace_id})
WHERE cr >= 5
WITH DISTINCT sender.acct_no AS acct_no
ORDER BY acct_no ASC
LIMIT 5
RETURN acct_no
LIMIT $limit
```

*call 2 → `ok`*, 5 rows, 11,405 db hits, 25 ms

```cypher
MATCH (sender:Account {_workspace_id: $workspace_id})-[t:TRANSFER]->(target:Account {_workspace_id: $workspace_id, acct_no: $a})
WHERE t.channel_risk >= 5
WITH DISTINCT sender.acct_no AS acct_no
ORDER BY acct_no ASC
LIMIT 5
RETURN acct_no
LIMIT $limit
```

*final reply* (tail):

```
ANSWER: [0,1,2,4,5]
```

*verdict*: `{"correct": true, "f1": 1.0, "recall": 1.0, "precision": 1.0, "gold": ["0", "1", "2", "4", "5"]}`

---

## 2. When the gate fires — `int_hard_1` at SF100, condition 4

The question asks for account pairs whose owners *guarantee one another*. The
model reads that as mutual — and the graph holds 40,001 GUARANTEE edges with not
one reciprocal pair, so the correct query for that reading returns nothing. Eight
round trips: two full executions at 11.2M db hits each, two plan refusals, one
`accept-cost` override that then times out at 60 s, three more attempts, and no
answer. The failure is the *question's* ambiguity, and `int_hard_1b` — the same
question with "in either direction" — is kept beside it as the control.

This is also the honest limit of the plan gate: it refuses queries, and refusing
a query that cannot be made cheap does not produce an answer.

**plan** · `int_hard_1` · SF100 · anchor None — WRONG (f1 0.0), 8 round trips, 125610 ms wall

*call 1 → `ok`*, 0 rows, 11,207,545 db hits, 4511 ms

```cypher
MATCH (m:Medium {_workspace_id: $workspace_id})-[:SIGN_IN]->(a1:Account {_workspace_id: $workspace_id})
MATCH (m)-[:SIGN_IN]->(a2:Account {_workspace_id: $workspace_id})
WHERE a1 <> a2 AND a1.acct_no < a2.acct_no
MATCH (a1)-[:TRANSFER]-(a2)
MATCH (o1)-[:OWN]->(a1)
MATCH (o2)-[:OWN]->(a2)
WHERE o1 <> o2
MATCH (o1)-[:GUARANTEE]->(o2)
MATCH (o2)-[:GUARANTEE]->(o1)
RETURN a1.acct_no AS acct_no1, a2.acct_no AS acct_no2
LIMIT $limit
```

*call 2 → `ok`*, 0 rows, 11,207,545 db hits, 4657 ms

```cypher
MATCH (m:Medium {_workspace_id: $workspace_id})-[:SIGN_IN]->(a1:Account {_workspace_id: $workspace_id})
MATCH (m)-[:SIGN_IN]->(a2:Account {_workspace_id: $workspace_id})
WHERE a1 <> a2 AND a1.acct_no < a2.acct_no
MATCH (a1)-[:TRANSFER]-(a2)
MATCH (o1)-[:OWN]->(a1)
MATCH (o2)-[:OWN]->(a2)
WHERE o1 <> o2
MATCH (o1)-[:GUARANTEE]->(o2)
MATCH (o2)-[:GUARANTEE]->(o1)
RETURN DISTINCT a1.acct_no AS acct_no1, a2.acct_no AS acct_no2
LIMIT $limit
```

*call 3 → `plan_rejected`*, 0 rows, 0 db hits, 2392 ms

```cypher
MATCH (a1:Account {_workspace_id: $workspace_id})-[:TRANSFER]-(a2:Account {_workspace_id: $workspace_id})
WHERE a1.acct_no < a2.acct_no
MATCH (o1:Person|Company {_workspace_id: $workspace_id})-[:OWN]->(a1)
MATCH (o2:Person|Company {_workspace_id: $workspace_id})-[:OWN]->(a2)
WHERE o1 <> o2
MATCH (o1)-[:GUARANTEE]->(o2)
MATCH (o2)-[:GUARANTEE]->(o1)
MATCH (m:Medium {_workspace_id: $workspace_id})-[:SIGN_IN]->(a1)
MATCH (m)-[:SIGN_IN]->(a2)
RETURN DISTINCT a1.acct_no AS acct_no1, a2.acct_no AS acct_no2
LIMIT $limit
```

*call 4 → `plan_rejected`*, 0 rows, 0 db hits, 2773 ms

```cypher
MATCH (a1:Account {_workspace_id: $workspace_id})-[:TRANSFER]->(a2:Account {_workspace_id: $workspace_id})
WHERE a1.acct_no < a2.acct_no
MATCH (o1:Person|Company {_workspace_id: $workspace_id})-[:OWN]->(a1)
MATCH (o2:Person|Company {_workspace_id: $workspace_id})-[:OWN]->(a2)
WHERE o1 <> o2 AND EXISTS { MATCH (o1)-[:GUARANTEE]->(o2) } AND EXISTS { MATCH (o2)-[:GUARANTEE]->(o1) }
AND EXISTS { MATCH (m:Medium {_workspace_id: $workspace_id})-[:SIGN_IN]->(a1) MATCH (m)-[:SIGN_IN]->(a2) }
RETURN DISTINCT a1.acct_no AS acct_no1, a2.acct_no AS acct_no2
LIMIT $limit
```

*call 5 → `timeout`*, 0 rows, 0 db hits, 60733 ms

```cypher
/* accept-cost */
MATCH (m:Medium {_workspace_id: $workspace_id})-[:SIGN_IN]->(a1:Account {_workspace_id: $workspace_id})
MATCH (m)-[:SIGN_IN]->(a2:Account {_workspace_id: $workspace_id})
WHERE a1.acct_no < a2.acct_no
AND (a1)-[:TRANSFER]-(a2)
MATCH (o1:Person|Company {_workspace_id: $workspace_id})-[:OWN]->(a1)
MATCH (o2:Person|Company {_workspace_id: $workspace_id})-[:OWN]->(a2)
WHERE o1 <> o2 AND (o1)-[:GUARANTEE]->(o2) AND (o2)-[:GUARANTEE]->(o1)
RETURN DISTINCT a1.acct_no AS acct_no1, a2.acct_no AS acct_no2
LIMIT $limit
```

*call 6 → `ok`*, 0 rows, 11,486,208 db hits, 4553 ms

```cypher
MATCH (o1:Person|Company {_workspace_id: $workspace_id})-[:GUARANTEE]->(o2:Person|Company {_workspace_id: $workspace_id})
WHERE (o2)-[:GUARANTEE]->(o1)
MATCH (o1)-[:OWN]->(a1:Account {_workspace_id: $workspace_id})
MATCH (o2)-[:OWN]->(a2:Account {_workspace_id: $workspace_id})
WHERE a1.acct_no < a2.acct_no
MATCH (a1)-[:TRANSFER]-(a2)
MATCH (m:Medium {_workspace_id: $workspace_id})-[:SIGN_IN]->(a1)
MATCH (m)-[:SIGN_IN]->(a2)
RETURN DISTINCT a1.acct_no AS acct_no1, a2.acct_no AS acct_no2
LIMIT $limit
```

*call 7 → `ok`*, 0 rows, 11,486,208 db hits, 4576 ms

```cypher
MATCH (o1:Person|Company {_workspace_id: $workspace_id})-[:GUARANTEE]->(o2:Person|Company {_workspace_id: $workspace_id})
WHERE (o2)-[:GUARANTEE]->(o1) AND o1 <> o2
MATCH (o1)-[:OWN]->(a1:Account {_workspace_id: $workspace_id})
MATCH (o2)-[:OWN]->(a2:Account {_workspace_id: $workspace_id})
WHERE a1.acct_no < a2.acct_no
MATCH (a1)-[:TRANSFER]-(a2)
MATCH (m:Medium {_workspace_id: $workspace_id})-[:SIGN_IN]->(a1)
MATCH (m)-[:SIGN_IN]->(a2)
RETURN DISTINCT a1.acct_no AS acct_no1, a2.acct_no AS acct_no2
LIMIT $limit
```

*call 8 → `ok`*, 0 rows, 979,722 db hits, 527 ms

```cypher
MATCH (o1:Person|Company {_workspace_id: $workspace_id})-[:GUARANTEE]->(o2:Person|Company {_workspace_id: $workspace_id})
WHERE (o2)-[:GUARANTEE]->(o1) AND o1 <> o2
MATCH (o1)-[:OWN]->(a1:Account {_workspace_id: $workspace_id})
MATCH (o2)-[:OWN]->(a2:Account {_workspace_id: $workspace_id})
WHERE a1.acct_no < a2.acct_no
AND EXISTS { MATCH (a1)-[:TRANSFER]-(a2) }
AND EXISTS { MATCH (m:Medium {_workspace_id: $workspace_id})-[:SIGN_IN]->(a1) MATCH (m)-[:SIGN_IN]->(a2) }
RETURN DISTINCT a1.acct_no AS acct_no1, a2.acct_no AS acct_no2
LIMIT $limit
```

*final reply* (tail):

```
(empty — the model returned no content)
```

*verdict*: `{"correct": false, "f1": 0.0, "note": "unparseable"}`

