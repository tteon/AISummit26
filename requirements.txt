# The ontology, guardrail and Agents SDK adapter this experiment measures — and the feature
# it produced: the tag includes seocho's row_format option (tteon/seocho#466), which is the
# CSV encoding condition 7 measured here, upstreamed. Pinned to a tag, not a branch, so this
# line keeps resolving to the same tree after upstream moves on. `python
# scripts/smoke_seocho.py` verifies an installed seocho against this repo's ontology.
seocho>=0.2.0           # SEOCHO Ontology, Guardrail, and Text2Cypher core
duckdb>=0.10          # deterministic graph generation
pyarrow>=14           # Parquet snapshots the bulk loader reads
neo4j>=5.20           # bolt driver
neo4j-rust-ext>=6.1   # the rust PackStream decoder the driver benchmarks compare against
openai-agents>=0.10   # the agent loop; also pulls openai
openai>=1.40
matplotlib>=3.8       # charts
pyyaml>=6
python-dotenv>=1.0
