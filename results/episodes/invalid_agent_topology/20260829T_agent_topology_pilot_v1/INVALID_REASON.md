This interrupted pilot is not evidence and must not enter an aggregate.

The runner generated paid MARA responses for two rejected Text2Cypher attempts on
`int_med_1`, but its outer exception record reported zero model calls and zero tokens. The
run was stopped immediately after the defect appeared. Raw samples, conversations, manifest,
and spans are retained here for audit; there is intentionally no completed report or trace
receipt. The replacement run uses a new run ID and records partial usage on every failure.
